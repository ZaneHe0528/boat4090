# 警戒线中线导航改进方案

## 背景

已有 `Boundary_identification.py` 能从 SegFormer 分割结果中提取左右两侧警戒线折线。目标：以两线中线作为小船航行路径，并改进该方案的工程可靠性。

---

## 改进项 1：逆透视映射（IPM / Bird's Eye View）⭐⭐⭐

### 问题
相机透视畸变导致像素坐标下的"中线"不等于真实世界的几何中线：
- 图像底部（近处）1 像素 ≈ 几厘米
- 图像顶部（远处）1 像素 ≈ 几米
- 逐行取左右 x 均值，结果在几何上偏向近端边界

### 方案
1. 用棋盘格标定相机，计算单应性矩阵 H（地面平面 → 图像平面）
2. 对每帧先做 `cv2.warpPerspective` 得到鸟瞰图
3. 在鸟瞰图中做中线计算，再将结果反变换回图像坐标用于显示
4. `path_processor.py` 中 `set_camera_calibration(homography_matrix=H)` 接口已预留，补充标定文件即可

### 实现要点
- 标定时在水面铺已知尺寸的棋盘格（或测量地面特征点）
- 鸟瞰图分辨率建议 1 pixel = 1~2 cm，覆盖前方 3~6 m 范围
- 倒影问题（水面反射）在鸟瞰图中表现为对称噪声，更易过滤

---

## 改进项 2：横向偏差时序滤波（EMA / Kalman）⭐⭐⭐

### 问题
分割模型逐帧输出有微小抖动，导致中线横向位置抖动 → 舵角频繁振荡。

### 方案
对**横向偏差（cross-track error, CTE）** 做指数滑动平均（EMA）：

```python
alpha = 0.3  # 越小越平滑，越大越响应快
cte_filtered = alpha * cte_raw + (1 - alpha) * cte_prev
```

或升级为 Kalman 滤波（状态：[cte, cte_velocity]），在观测噪声较大时效果更优。

### 在哪里加
在 `realtime_pilot.py` 的帧循环中，拿到 `center_pts` 后、传给 Pure Pursuit 之前，对图像底部中心点的 x 偏差做滤波。`trajectory_planner.py` 中的 `path_history` 机制可扩展为此用途。

---

## 改进项 3：近端 ROI 截断⭐⭐

### 问题
图像顶部（远端）边界检测可靠性低（透视压缩、波光干扰），将其纳入中线计算引入噪声。

### 方案
只使用图像下方 50%（可调）的行做中线计算：

```python
ROI_TOP_RATIO = 0.5   # 只用 y > img_h * 0.5 的行
effective_rows = [y for y in overlap_ys if y > img_h * ROI_TOP_RATIO]
```

`plan_path_from_bottom` 已有类似逻辑，可直接复用。

---


## 改进项 4：边界样条拟合（可选）⭐

### 问题
逐行扫描的折线点在局部仍有锯齿。

### 方案
用 `scipy.interpolate.splprep` 对左右边界折线做样条拟合后再取中线。`path_processor.py` 中已有 `path_smoothing_factor` 参数和样条插值代码，对边界线复用即可。

---

## 推荐实施路线

```
阶段 1（1-2天）：近端 ROI 截断 + EMA 时序滤波
  → 最低成本，立刻减少抖动

阶段 2（3-5天）：相机标定 + IPM 鸟瞰图
  → 解决几何正确性，是最重要的根本性改进

阶段 3（1天）：航道宽度监控
  → 安全裕度感知，驱动速度策略
```

---

## 关键文件索引

| 文件 | 作用 |
|------|------|
| `scripts/Boundary_identification.py` | 警戒线提取与可视化（当前入口） |
| `scripts/plan_path.py` | 三级回退路径规划（中线计算参考） |
| `scripts/realtime_pilot.py` | 端到端导航（帧循环，加滤波的位置） |
| `river_lane_pilot/planning/path_processor.py` | 坐标变换、样条平滑（IPM 接口预留处） |
| `river_lane_pilot/planning/trajectory_planner.py` | 轨迹规划器（path_history 可扩展为 EMA） |
| `river_lane_pilot/planning/pure_pursuit.py` | Pure Pursuit 控制器 |
